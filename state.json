{
  "version": 1,
  "objective": {
    "problem": "Decide whether individually weak IdrEvent signals form a credible campaign when their order and infrastructure relationships are evaluated together.",
    "decision_target": "Advisory IntelligenceFinding (campaign probability + ranked entities + evidence event IDs) consumed by idr-sentinel deterministic corroboration.",
    "success_metrics": [
      "Held-out ROC-AUC / PR-AUC / Brier under chronological 60/20/20 splits",
      "S6+GNN hybrid must beat or match every ablation variant on Brier under the same split",
      "Every finding carries evidence_event_ids resolvable to source telemetry"
    ]
  },
  "constraints": [
    "Advisory boundary: model output must never trigger PanicResponse; idr-sentinel corroborates against primary telemetry first",
    "Latent state is never an auditable substitute for source events; evidence IDs are mandatory in findings",
    "Chronological splits only; no training on future information relative to prediction time",
    "Synthetic benchmark results must never be presented as production detection accuracy",
    "python >= 3.11; deps: numpy, torch >= 2.2, scikit-learn; GPL-3.0-or-later"
  ],
  "inputs": {
    "sources": [
      "NDJSON streams of idr_common::IdrEvent from idr-main; synthetic matched campaigns from simulator.py"
    ],
    "schemas": [
      "IdrEvent: {id, timestamp, source, severity, kind{type,...}, metadata} \u2014 validated in schema.py"
    ],
    "provenance": [
      "Simulator seeds are deterministic; event IDs are seed-derived UUIDs"
    ]
  },
  "outputs": [
    "IntelligenceFinding JSON (pipeline.py): campaign_id, escalation_probability (affine-calibrated), raw_escalation_probability, calibration, predicted_next_stage (ATT&CK), observed_attack_stages, related_entities, evidence_event_ids, model_version, graph_nodes, graph_relations, engine_version, feature_schema_hash, scored_at, feature_drift, continues_campaign, windows_observed; entity_evidence (per-entity why: occlusion features, typed edges, ATT&CK); applied_suppressions",
    "Ablation benchmark report (training.py, reports/demo.json) with per-scenario generalization and rolling-origin CV verdict",
    "Checkpoint artifacts/hybrid_model.pt (dict: state_dict incl. temperature+cal_bias buffers, dims, use_s6/use_gnn/pooling, manifest, feature_stats)"
  ],
  "architecture": {
    "components": [
      {
        "module": "src/idr_intelligence/schema.py",
        "role": "IdrEvent validation, severity/kind priors"
      },
      {
        "module": "src/idr_intelligence/features.py",
        "role": "entity extraction (host/process/hash/ip/prefix/asn/domain/device + user/session/cloud identity), typed edges, 22-dim feature vector"
      },
      {
        "module": "src/idr_intelligence/graph.py",
        "role": "temporal graph: per-entity histories, per-entity deltas, time-decayed edges, node budget, typed edges, evidence provenance"
      },
      {
        "module": "src/idr_intelligence/models.py",
        "role": "SelectiveSSM (step() cell + hoisted forward), ResidualGraphLayer, GatedAttentionPool, CampaignModel, checkpoint save/load"
      },
      {
        "module": "src/idr_intelligence/simulator.py",
        "role": "11 scenario families incl. stale_preamble & timing_only (temporal-physics isolation tests); per-event scenario + stage_index ground truth"
      },
      {
        "module": "src/idr_intelligence/training.py",
        "role": "chronological four-way ablation trainer (HIDDEN_DIM=24, STATE_DIM=6)"
      },
      {
        "module": "src/idr_intelligence/pipeline.py",
        "role": "evidence-linked scoring"
      },
      {
        "module": "src/idr_intelligence/cli.py",
        "role": "demo + NDJSON score commands"
      },
      {
        "module": "src/idr_intelligence/config.py",
        "role": "EngineConfig: typed single home for tunables + prior tables + config hash + TOML loader"
      },
      {
        "module": "src/idr_intelligence/registry.py",
        "role": "ModelManifest, feature_schema_hash, SchemaMismatchError"
      },
      {
        "module": "src/idr_intelligence/attack.py",
        "role": "deterministic kind->ATT&CK tactic/technique table, observed stages, next-stage prediction"
      },
      {
        "module": "src/idr_intelligence/benchmark.py",
        "role": "frozen-manifest benchmark runner with regression floors (benchmarks/v1.json)"
      },
      {
        "module": "src/idr_intelligence/dataio.py",
        "role": "*.labeled.ndjson loader for LabeledWindow real-campaign ingestion"
      },
      {
        "module": "src/idr_intelligence/bounded_graph.py",
        "role": "GraphBudget + audited LRU EvictionRecord for bounded node memory (W15 streaming infra)"
      },
      {
        "module": "src/idr_intelligence/evidence.py",
        "role": "structured per-entity evidence (occlusion attribution, typed edges, ATT&CK) + suppression allowlist"
      },
      {
        "module": "src/idr_intelligence/campaigns.py",
        "role": "cross-window campaign identity: durable-entity fingerprints, weighted-Jaccard matcher, content-addressed IDs, JSON-persisted CampaignRegistry"
      },
      {
        "module": "src/idr_intelligence/streaming.py",
        "role": "one-event-at-a-time scoring: EntityState carried S6 (state,output) via step(), bounded by GraphBudget with audited eviction; relational head re-run at query time"
      },
      {
        "module": "src/idr_intelligence/export.py",
        "role": "ONNX bundle export (step cell + relational head + manifest of every scoring constant) and OnnxStreamScorer, the torch-free reference runner that is the executable spec of the serving contract"
      },
      {
        "module": "rust/idr-intelligence-rt/src/scorer.rs",
        "role": "Rust streaming scorer over tract: semantics-preserving port of streaming.py pinned to Python by committed golden streams"
      },
      {
        "module": "rust/idr-intelligence-rt/src/features.rs",
        "role": "Rust port of features.project_event (entities/edges/22-dim vector incl. blake2s kind hash), pinned exactly by feature_vectors.json fixture"
      },
      {
        "module": "rust/idr-common-parity/tests/wire.rs",
        "role": "machine-local wire parity: bridge parses idr_common-serialized events, goldens deserialize into real idr_common::IdrEvent (not in CI; path-dep on idr-main tree)"
      },
      {
        "module": "src/idr_intelligence/observability.py",
        "role": "opt-in structured stderr logging (JSON lines): model provenance on load, per-run counts/timing, finding summaries, drift flags, evictions, rejections; silent by default so the stdout finding contract and determinism are untouched"
      },
      {
        "module": "src/idr_intelligence/validation.py",
        "role": "real-data go/no-go validation gate: recalibrate + select an operating threshold at a target FPR on an earlier time segment, snapshot a real drift baseline, verify metrics + realized FPR on a strictly-later untouched segment, turn configurable gates into a verdict + model card; verdict is BINDING only on operator-attested real provenance (synthetic runs are explicit wiring checks)"
      },
      {
        "module": "src/idr_intelligence/attack.py",
        "role": "deterministic kind->ATT&CK mapping + next-stage prediction, now grounded in real MITRE ATT&CK: TACTIC_ORDER derived from the canonical enterprise matrix (14 tactics), KIND_TO_ATTACK validated technique-and-tactic-consistent against the real dataset by test, technique_name() enrichment"
      }
    ],
    "interfaces": [
      {
        "name": "IdrEvent",
        "version": "v1",
        "contract": "canonical tagged shape from idr_common; kind must contain 'type'"
      },
      {
        "name": "checkpoint",
        "version": "v4",
        "contract": "state_dict (incl. temperature + cal_bias buffers), feature_dim/hidden_dim/state_dim, use_s6/use_gnn/pooling, manifest (engine_version, feature_schema_hash, config_hash, created_at, calibration string), feature_stats; loader defaults missing temperature/cal_bias to identity and raises SchemaMismatchError on feature-schema drift; raw v1 state dicts still load; also carries time_mode (global/per_entity/time_aware); time-aware S6 adds a time_weight param (absent otherwise); also carries decay_half_life (None = binary edges)"
      },
      {
        "name": "TemporalGraph",
        "version": "v1",
        "contract": "node_ids, sequences [N,steps,22], mask, deltas [N,steps] (per-entity log1p gaps), symmetric degree-normalized adjacency, evidence_ids per node, relation_counts, typed_edges (node,node,relation), evictions (audited node-budget drops)"
      },
      {
        "name": "IntelligenceFinding",
        "version": "v7",
        "contract": "v6 fields + continues_campaign (bool) and windows_observed (int), populated when score_events receives a CampaignRegistry; without one they default to False/1 and campaign_id stays the per-call first-event derivation \u2014 additive, back-compat"
      },
      {
        "name": "CampaignRegistry",
        "version": "v1",
        "contract": "JSON {records: [{campaign_id, fingerprint {entity: weight}, windows_observed, first_seen, last_seen}]}; match_or_register(entities, scored_at) -> (campaign_id, continues, windows): weighted Jaccard >= 0.5 over durable entities (process:/session: excluded; hash 3.0 > domain/user 2.5 > ip/cloud 2.0 > prefix/asn 1.5 > device 1.0 > host 0.5) continues and unions the best match, else registers a blake2s content-addressed id; empty fingerprint -> sentinel idr-campaign-unfingerprinted, never registered"
      },
      {
        "name": "LabeledWindow",
        "version": "v1",
        "contract": "{window_id, label 0|1, events [IdrEvent,...]} one-per-line in *.labeled.ndjson; loader sorts by window start so the chronological split stays honest; single-class split segments fail loudly"
      },
      {
        "name": "OnnxExportBundle",
        "version": "v1 (format idr-intelligence-onnx-v1)",
        "contract": "step.onnx (x_t[1,F], state[1,H,S], output[1,H], delta_t[1] iff time_aware -> state_out, output_out) + head.onnx (outputs[N,H], adjacency[N,N] -> graph_logit[1], node_logits[N], N dynamic) + manifest.json carrying engine/model versions, feature_schema_hash, dims/time_mode/decay, calibration (temperature/bias/label), severity_weight + kind_prior tables with defaults, delta_log_divisor, ATT&CK tactic_order + kind_to_attack, top_k/evidence_limit, optional feature_stats, and per-graph IO signatures. Consumers hardcode nothing; the Python exporter is the single source of truth"
      },
      {
        "name": "ValidationReport",
        "version": "v1 (report idr-intelligence-validation-v1)",
        "contract": "validate_model(model, windows, target_fpr, holdout, provenance, gates) -> report dict: verdict go|no-go, binding bool (real-provenance AND passed), operating_threshold {tau, dev_fpr, dev_tpr}, calibration {label,scale,bias}, data {dev/test window+positive counts, holdout}, test_metrics (roc_auc/pr_auc/brier/ece/recall@fpr), test_confusion_at_threshold (realized fpr/tpr/precision), drift_baseline, gates[] each {name,value,op,threshold,pass}. Mutates the model to carry real calibration + real drift baseline; CLI writes the recalibrated checkpoint ONLY on go and exits 2 on no-go"
      }
    ],
    "decisions": [
      {
        "id": "ADR-1",
        "decision": "Advisory-only integration; findings feed idr-sentinel, never actuate responses",
        "where": "docs/ARCHITECTURE.md"
      },
      {
        "id": "ADR-2",
        "decision": "Explicit per-step S6 scan chosen for auditability over throughput in v0.1",
        "where": "reports/AUDIT.md"
      },
      {
        "id": "ADR-3",
        "decision": "Masked max-pools fill with -inf so isfinite guards genuinely zero all-padding rows (was -1e9 no-op)",
        "where": "PR #6"
      },
      {
        "id": "ADR-4",
        "decision": "Ablation variants construct only the submodules they use, keeping parameter counts honest",
        "where": "PR #6"
      },
      {
        "id": "ADR-5",
        "decision": "Checkpoints carry their own dimensions; CLI must not hardcode hyperparameters",
        "where": "PR #6"
      },
      {
        "id": "ADR-6",
        "decision": "campaign_id derives from chronologically first event, independent of caller ordering",
        "where": "PR #6"
      },
      {
        "id": "ADR-7",
        "decision": "Push CI restricted to main; PR commits build once via pull_request trigger",
        "where": "PR #6"
      },
      {
        "id": "ADR-8",
        "decision": "mypy follows the running interpreter version; pinning 3.11 breaks against numpy 2.5 PEP 695 stubs on newer Pythons",
        "where": "PR #6"
      },
      {
        "id": "ADR-9",
        "decision": "Gated MIL attention pooling replaces mean+max in the default model: attention scores are the entity ranking, so evidence selection finally receives training gradient (node_head previously had grad=None). uniform_pool retained as fifth ablation arm; uniform path stays numerically identical to legacy checkpoints",
        "where": "docs/ROADMAP.md W1"
      },
      {
        "id": "ADR-10",
        "decision": "Design roadmap adopted: 4 phases / 14 workstreams synthesized from 24 judged proposals; docs/ROADMAP.md is canonical. Rejected: learned next-stage head (circular supervision), dense R-GCN typed adjacency (unfalsifiable pre-Phase-2)",
        "where": "docs/ROADMAP.md"
      },
      {
        "id": "ADR-11",
        "decision": "Checkpoints persist ablation flags and pooling mode so every trained variant round-trips (verify-workflow finding); non-dict payloads rejected with a clear error",
        "where": "models.py save_checkpoint/load_campaign_model"
      },
      {
        "id": "ADR-12",
        "decision": "All tunables live in config.EngineConfig (TOML-loadable, hashable); prior tables moved to config with schema.py re-export; ENGINE_VERSION defined in config to avoid the __init__ import cycle. Checkpoints embed a ModelManifest and refuse to load across feature-schema changes",
        "where": "docs/ROADMAP.md W2"
      },
      {
        "id": "ADR-13",
        "decision": "Temperature calibration fitted by validation-NLL grid search with T=1 always in the grid, making 'post-scaling validation NLL never worsens' a hard CI-assertable guarantee (the roadmap's ECE<=pre wording is not guaranteeable by NLL fitting). Findings report raw + calibrated probability + calibration string; ECE/log-loss/MCE added to ablation metrics",
        "where": "docs/ROADMAP.md W3"
      },
      {
        "id": "ADR-14",
        "decision": "Dataset labels are seeded random draws with a deterministic retry guaranteeing both classes in every chronological segment (the former index%2 alternation leaked parity across split boundaries); _fit weights BCE by neg/pos, calibration stays unweighted so probabilities match true frequencies; _evaluate adds recall@FPR{1%,0.1%} and precision@5",
        "where": "docs/ROADMAP.md W4"
      },
      {
        "id": "ADR-15",
        "decision": "Next-stage stays deterministic per the roadmap's rejection of the learned head: attack.py maps 11 of 12 kinds to ATT&CK tactic/technique (triage_classification is a correlator meta-event, deliberately unmapped); prediction advances past the furthest observed tactic, fixing the old presence-lookup's order blindness",
        "where": "docs/ROADMAP.md W5"
      },
      {
        "id": "ADR-16",
        "decision": "Simulator scenarios are deterministic in (label, seed, scenario) with no RNG object; every scenario keeps the same six kill-chain kinds so nothing separates classes except convergence/order/semantics; train_ablation reports scenario_generalization for the trained hybrid across all families",
        "where": "docs/ROADMAP.md W6"
      },
      {
        "id": "ADR-17",
        "decision": "benchmarks/v1.json freezes generator config + floors; scenario floors are Brier-only because ranking metrics observably flip (distractor ROC 0.0<->1.0) at CI budgets and would flake. CI runs the manifest every build. Checkpoints embed per-feature training histograms; scoring attaches advisory PSI drift (0.2 flag line) to findings",
        "where": "docs/ROADMAP.md W7"
      },
      {
        "id": "ADR-18",
        "decision": "Real labeled windows enter via --data with zero model changes (windows_to_batch reuses build_temporal_graph); the report records data_source and the observed malicious rate; simulator diagnostics (evidence precision, scenario generalization) still run as synthetic sidecars",
        "where": "docs/ROADMAP.md W8"
      },
      {
        "id": "ADR-19",
        "decision": "rolling_origin_ablation: expanding-window folds (40% minimum origin, 15% val/test blocks, folds<=3) x seed replicates; Brier is the decision metric (ranking saturates); best_model declared only when the paired margin exceeds the paired per-fold std, else 'tie'. First run confirms the roadmap's claim: 9-fold verdict is tie (margin 0.024 vs std 0.124) \u2014 single-split winners were noise",
        "where": "docs/ROADMAP.md W9"
      },
      {
        "id": "ADR-20",
        "decision": "Calibration is affine (scale + bias), not temperature-only: adversarial verification proved a pure scale cannot undo the constant log(pos_weight) shift that class-weighted training introduces, so calibrated probabilities were inflated under imbalance. affine_calibration_params fits both by LBFGS from identity, bounded to scale in [0.2,5] and bias in [-5,5] to prevent separable-data runaway, accepted only if validation NLL does not worsen. Single calibration_label() method shared by checkpoint and finding. cal_bias buffer added; older checkpoints default it to 0",
        "where": "verification wk3kr3oza finding #1 (blocking)"
      },
      {
        "id": "ADR-21",
        "decision": "Identity entities: user: (global \u2014 the same actor links events across hosts, the load-bearing lateral-movement pivot), session:{host}:{sid}, cloud: nodes with authenticates_to/owns/spawns/accesses edges; has_user + has_identity_pivot features (FEATURE_DIM 20->22, schema hash rotates, checkpoints regenerate). lateral_movement scenario scatters all infrastructure so only the shared user connects the campaign; cross-host linkage is structural (global user node), not a per-event feature",
        "where": "docs/ROADMAP.md W10 \u2014 closes Phase 2"
      },
      {
        "id": "ADR-22",
        "decision": "W11 per-entity time: build_temporal_graph tracks last_seen per entity; under per_entity/time_aware the delta_seconds_log feature becomes the gap since THAT entity was last seen (feature values change but FEATURE_NAMES/schema-hash do not), and a deltas [N,steps] channel carries per-entity gaps to a time-aware SelectiveSSM (delta += time_weight*dt, time_weight init 0 = identity). HONEST RESULT: time_ablation on low_and_slow at 80/3 gives global Brier 0.221 vs per_entity/time_aware 0.238 \u2014 time-awareness does not beat the global gap at demo budget (time_weight barely moves in 3 epochs; per-entity resets lose graph-level timing signal). Capability shipped with a falsifiable test; verdict recorded, not assumed",
        "where": "docs/ROADMAP.md W11"
      },
      {
        "id": "ADR-23",
        "decision": "W12 delivers the two FALSIFIABLE parts of the roadmap's bounded-decayed-sparse graph: time-decayed edge weights (0.5**(age/half_life), off by default) with a distractor ablation, and GraphBudget+EvictionRecord audited node eviction (unit-tested, never triggers at demo scale). DEFERRED to W15 (streaming): torch.sparse.mm (pure overhead at <=24 nodes) and the build_temporal_graph->replay-wrapper refactor (needs streaming as a second consumer) \u2014 shipping either now would be unfalsified complexity, per the roadmap's own R-GCN rejection logic. HONEST RESULT: decay_ablation on distractor gives none/1h/15m Brier 0.2366/0.2366/0.2364 \u2014 decay barely moves because distractor plants benign edges contemporaneously, not as a stale preamble; the mechanism is correct but this scenario does not stress it. decay_half_life defaults None (shipped hybrid unchanged; floors stable)",
        "where": "docs/ROADMAP.md W12"
      },
      {
        "id": "ADR-24",
        "decision": "W14 structured evidence: the finding keeps its flat top-k for Rust back-compat but adds entity_evidence \u2014 per ranked entity, the events implicating it, typed edges to other ranked entities (graph.py now retains per-pair typed_edges, previously only counted), top-3 features by occlusion attribution (zero each of 22 channels, measure node-logit drop), and ATT&CK techniques from its events. All derived from the existing graph/model, so nothing bypasses the evidence boundary. apply_suppressions attenuates allowlisted entities (exact id or 'prefix:') out of the RANKING only \u2014 campaign probability untouched, finding never hidden from idr-sentinel, applied_suppressions audited",
        "where": "docs/ROADMAP.md W14 \u2014 chosen as best-ROI: completes the actionable-evidence product, dodges the synthetic-benchmark wall that stalled W11/W12"
      },
      {
        "id": "ADR-25",
        "decision": "Benchmark hardening to validate W11/W12 (user directive 'fix the benchmark first'): added stale_preamble (identical benign preamble ~2 days before the recent window) and timing_only (identical structure/content, ONLY inter-event timing differs) scenarios. FINDING: temporal physics is not empirically differentiated on synthetic data. On timing_only every mode separates at ROC/PR 1.0 and global wins Brier decisively (0.186 vs 0.210) \u2014 the scalar delta_seconds_log feature already encodes timing, so per-entity clock + time-aware S6 + edge decay add variance without signal. Documented in reports/AUDIT.md. Recommends flipping the shipped time_mode default from time_aware to global (simpler + better); deferred to keep this a pure benchmark/doc addition. Validating temporal physics needs real labeled campaigns (--data path), not more synthetic scenarios",
        "where": "reports/AUDIT.md, docs/ROADMAP.md"
      },
      {
        "id": "ADR-26",
        "decision": "Flipped the shipped CampaignModel default time_mode from time_aware to global (and build_temporal_graph default likewise), acting on ADR-25's decisive evidence that global >= time_aware on every synthetic scenario and the delta feature already captures timing. The simpler mode is now default; per_entity/time_aware stay opt-in for real-data deployments. decay_half_life already defaulted None. Benchmark floors re-verified green with the global hybrid",
        "where": "reports/AUDIT.md consequence 2; user-delegated call"
      },
      {
        "id": "ADR-27",
        "decision": "W13 S6 step() cell: SelectiveSSM refactored so a single _recurrence path is shared by forward (loops it with input_proj hoisted over the whole sequence) and a public step(x_t, state, output, active, delta_t) cell for streaming/export. initial_state() gives a fresh (state, output); snapshot/restore is lossless. Numerical-equivalence test asserts looping step() == forward for both time modes; forward output is bit-identical to the pre-refactor loop so benchmark floors are unchanged. This is the prerequisite for W15 streaming and W17 ONNX/Rust export (one code path, no train/serve skew). Heavy throughput work (associative scan, torch.compile) deferred per roadmap",
        "where": "docs/ROADMAP.md W13"
      },
      {
        "id": "ADR-28",
        "decision": "W16 campaign identity is deterministic set-matching, not a model: durable-entity fingerprints (weighted by continuity strength \u2014 hash/domain/user outweigh host; ephemeral process:/session: excluded) matched by weighted Jaccard at 0.5. A match continues the campaign (fingerprint unioned so identity follows infrastructure as it grows), a miss registers a blake2s content-addressed id (order-independent, reproducible). The registry is opt-in (score_events(registry=...), CLI --registry PATH load->match->save); without it findings behave exactly as before. Sentinel id for windows with no durable entities. Auditable by construction \u2014 idr-sentinel can recompute every match decision from the finding + registry JSON",
        "where": "docs/ROADMAP.md W16"
      },
      {
        "id": "ADR-29",
        "decision": "W15 streaming scorer: per-entity S6 (state,output) carried forward with the W13 step() cell \u2014 O(1) work and state per (event,entity), no history replay \u2014 while the GNN+pooling+heads run at query time via CampaignModel.relational_head(), extracted from forward() as pure code motion (benchmark floors confirm bit-identical). Adjacency/decay/drift/stages accumulate incrementally against factored shared helpers (graph.degree_normalize, pipeline.psi_drift, attack.next_stage_from_stages) so batch and streaming share one code path everywhere. Numerical-equivalence tests pin streaming==batch findings across all three time modes, decayed edges, and PSI drift. HONEST SCOPE: events must arrive chronologically (out-of-order raises; an upstream replay/sort buffer owns reordering \u2014 the W12-deferred replay-wrapper is that buffer, still deferred until a real consumer exists); entity_evidence is empty in streaming findings because occlusion attribution requires exactly the history replay streaming exists to avoid; incremental LRU eviction under a budget can differ from one-shot batch eviction (inherent \u2014 the stream cannot know the future); torch.sparse.mm still deferred \u2014 relational head cost is O(N^2 * hidden) per query at bounded N, dense bmm remains cheaper at these sizes",
        "where": "docs/ROADMAP.md W15"
      },
      {
        "id": "ADR-30",
        "decision": "W17 serving contract is two small ONNX graphs plus a constants manifest, not one monolithic model: the W13 step() cell exports with fixed shapes (recurrence driven by the consumer, no ONNX Scan/Loop), the shared relational_head exports with a symbolic node axis, and manifest.json carries every scoring constant so the Rust side hardcodes nothing. TorchScript exporter at opset 18 pinned deliberately (dynamo=False) - its op choices are what the parity suite validates. Runtime is tract (pure Rust, no ONNX Runtime shared lib, builds in the Arch CI container); ort rejected as a system-library dependency. One model change was required: GatedAttentionPool's nan_to_num became where(isnan,0) - identical semantics (softmax output is bounded so the inf branches were dead code) but avoids the IsInf op tract cannot evaluate; benchmark floors confirm no behavior change. export.OnnxStreamScorer (torch-free, onnxruntime) is the executable spec the Rust port is tested against; committed fixtures (two mixed multi-campaign golden streams + per-event feature vectors + budget/suppression case, generated by a script that asserts ORT==torch agreement and a 5e-5 top-k probability gap floor before writing) pin Rust==Python field-for-field with 1e-4 probability tolerance. Integration against idr_common::IdrEvent is proven by the machine-local rust/idr-common-parity crate (path-dep on the idr-main working tree; NOT in CI) because the published github leelupton/idr is stale - it lacks ExternalTriage/TriageClassification/BgpAnomaly that the local tree and this engine already handle, so a git-dep would pin an outdated schema. HONEST SCOPE mirrors streaming: no CampaignRegistry in Rust (first-event fallback id), entity_evidence empty, and findings are emitted as bare JSON in the sketched EventKind::IntelligenceFinding field shape - the envelope variant remains a proposed idr-main change awaiting explicit instruction (idr-main is modify-only-on-instruction)",
        "where": "docs/ARCHITECTURE.md; rust/idr-intelligence-rt/README.md"
      },
      {
        "id": "ADR-31",
        "decision": "Entity ranking uses a stable sort with first-seen tie-break everywhere (np.argsort kind=stable in pipeline/streaming/export; stable sort_by in Rust). Exact probability ties are data-inherent, not fixable by seeds: structurally identical entities (same feature history, isomorphic graph position) score identically under ANY weights, and numpy's default introsort made their ranked order an unspecified artifact. A defined tie order is required for findings to be reproducible across runs, platforms, and the Rust bridge. One pre-existing test loosened to set-comparison: a ~1e-8 batch-vs-streaming float difference can legitimately break an exact tie in only one path, permuting tied neighbors",
        "where": "src/idr_intelligence/pipeline.py; tests/test_system.py test_streaming_matches_batch_scoring"
      },
      {
        "id": "ADR-32",
        "decision": "Ground the deterministic ATT&CK machinery in real MITRE data. Lee pointed at /home/lee/Desktop/cti (the MITRE ATT&CK/CAPEC STIX repo). scripts/ground_attack_reference.py distills the 45MB Enterprise STIX bundle into a compact committed src/idr_intelligence/data/attack_reference.json (canonical 14-tactic order + 691 techniques id->{name,tactics}); attack.py derives TACTIC_ORDER from it and validates KIND_TO_ATTACK against it (validate_mapping_against_reference). Every one of the 11 hand-typed mappings was already tactic-consistent with real MITRE (validated, not assumed); the only gap was the hand TACTIC_ORDER omitting the two pre-compromise tactics (reconnaissance, resource-development) \u2014 added, which is behavior-preserving for next_stage because no kind maps to them and relative order is unchanged. A freshness test rebuilds from the live bundle when present so a MITRE release cannot leave the vendored copy stale. HONEST SCOPE: this grounds the DETERMINISTIC/auditable field idr-sentinel corroborates and can make the simulator more realistic; it is NOT labeled IdrEvent campaign data, so it does not close the training/calibration wall for the learned escalation model.",
        "where": "src/idr_intelligence/attack.py; scripts/ground_attack_reference.py; src/idr_intelligence/data/attack_reference.json"
      }
    ]
  },
  "tasks": {
    "completed": [
      "v0.1.0 package merged to main (PR #2); CI lint fix (PR #5); Ocean Reef takeoff migrated out to LeeLupton/pricing (PR #4, pricing#1)",
      "Review follow-ups + test hardening (4->14) + docstrings + mypy gate (PR #6)",
      "state.json bootstrap (this slice)",
      "Design roadmap synthesized and committed (docs/ROADMAP.md); state task graph now mirrors it",
      "Adversarial diff verification of PR #6 (19 agents): 4 distinct upheld findings, all fixed in the W1 slice (variant round-trip, legacy-shape test, vacuous order-invariance test, roundtrip weight fidelity)",
      "W1: attention-gated pooling + evidence_precision_at_5 metric + fifth ablation arm",
      "W2: EngineConfig + ModelManifest (config.py, registry.py, configs/default.toml, provenance fields on findings, schema-hash rejection on load)",
      "W3: temperature calibration (validation-NLL grid, T buffer in state_dict, manifest calibration string) + ECE/log-loss/MCE metrics + raw/calibrated finding fields",
      "W4: seeded random labels + pos_weight + recall@FPR and precision@k operating-point metrics; CLI --malicious-rate",
      "W5: ATT&CK mapping (attack.py) \u2014 Phase 1 complete",
      "W6: graded + adversarial simulator scenarios with per-scenario generalization table",
      "W7: versioned benchmark suite (benchmarks/v1.json, runner, CI gate) + feature-snapshot drift on findings",
      "W8: LabeledWindow schema + dataio loader + --data training path",
      "W9: rolling-origin CV + replicates with statistical best-model verdict (CLI: ablation)",
      "Verification fixes (wk3kr3oza, 12 upheld): affine calibration replacing broken temperature-only [BLOCKING], degenerate recall@FPR handling, Rust sketch + state.json contract sync [BLOCKING], attack docstring, and 5 vacuous/tautological tests rewritten to fail on real regressions [2 BLOCKING]",
      "W10: identity entities (user/session/cloud) + edges + features + lateral_movement scenario \u2014 Phase 2 complete",
      "W11: per-entity time deltas + time-aware S6 discretization + time_ablation (honest verdict: global wins on low_and_slow at demo budget)",
      "W12: time-decayed edges + GraphBudget audited eviction + decay_ablation (honest near-tie on distractor); sparse-mm and replay-wrapper deferred to W15",
      "W14: structured per-entity evidence (occlusion attribution, typed edges, ATT&CK per entity) + analyst suppression allowlist",
      "Benchmark hardening: stale_preamble + timing_only scenarios; VERIFIED temporal physics (W11/W12) undifferentiated on synthetic data (global beats time-aware on timing_only 0.186 vs 0.210); documented in AUDIT.md",
      "Flip default time_mode time_aware->global (evidence-driven, ADR-26); benchmark floors green; default locked by test",
      "v0.1 consolidation: README refreshed to match the shipped engine (capabilities, all CLI subcommands, full repo map, honest temporal-physics note, updated resume bullet)",
      "W13: S6 step() cell (shared _recurrence, streaming step(), lossless snapshot/restore, numerical equivalence to forward) \u2014 enables W15/W17",
      "W16: campaign-identity registry (campaigns.py; weighted durable-entity fingerprint matching, content-addressed IDs, opt-in via score_events(registry=)/CLI --registry; finding v7 adds continues_campaign + windows_observed)",
      "W15: streaming scorer (streaming.py: StreamingScorer over W13 step() + bounded_graph, CLI stream subcommand; equivalence to batch pinned across time modes/decay/drift)",
      "W17: ONNX export + Rust serving bridge (export.py + CLI export; rust/idr-intelligence-rt on tract, golden-pinned to StreamingScorer; rust/idr-common-parity wire proof against the real idr_common from the idr-main tree; CI extended with cargo fmt/clippy/test). ROADMAP COMPLETE - all 17 workstreams shipped",
      "W17 adversarial review (65-agent workflow: 5 lenses -> 30 raw findings -> 2 refuters each -> 19 confirmed) and fix slice: use_gnn=False export now declares/feeds the head's real input signature (nognn golden bundle added); OnnxStreamScorer verifies feature_schema_hash and reads evidence_limit from the manifest; manifest constants are imported from features.py (SEVERITY_DEFAULT/KIND_PRIOR_DEFAULT/DELTA_LOG_DIVISOR), test-tied; _masked_max avoids the IsInf op (uniform-pooling bundles tract-loadable, test-pinned); CampaignModel rejects non-positive decay_half_life; Rust truncates wire timestamps to microseconds (idr-main emits nanoseconds), accepts Python's falsy-metadata and common naive-ISO shapes, validates manifests (feature-name length, decay, stats shape) instead of panicking, and feeds the head per the manifest's input list; fixtures regenerated with content-addressed ids (campaign_id now discriminates), a real drift baseline (some-but-not-all features flag), budget-case gap checks, salt-searched tie-free rankings, and 9 hand-authored edge-case events covering every extraction branch; parity.rs adds field-set equality + manifest-rejection tests; ranked_indices extracted with tie unit tests both sides; freshness gates in test_system.py replay all committed goldens + feature vectors against the current engine so staleness fails CI. Deferred with documentation: per-event entity caps (parity with Python; trust boundary documented in the crate README), exotic ISO-8601 shapes, rolling Arch rust toolchain in CI (same class as the ruff pin \u2014 revisit on first breakage)",
      "Review closeout \u2014 the four partially-addressed/deferred confirmed findings are now fixed for real: (#17) both stream CLIs default to --max-nodes 4096 with 0=unbounded (engine APIs unchanged; symmetric Python/Rust; dense N x N adjacency bounded on untrusted streams); (#15) rust-toolchain.toml pins 1.96.1 at the repo root and CI installs rustup instead of Arch's rolling rust package (all cargo gates re-verified under 1.96.1); (#7) the Rust timestamp parser now accepts every shape Python's fromisoformat does (comma fractions, basic separator-free forms, hour-only precision, colon-less offsets), pinned by a 12-shape unit table and three new exotic-timestamp fixture events; (#14) Rust golden tolerance tightened 1e-4 -> 2e-6 (goldens are 6-decimal rounded so runtime drift is quantized; measured ORT-vs-golden diff 0.0, tract passes at 2e-6; pytest freshness stays 1e-4 because CI's ORT version floats). Rejected findings stay rejected (each refuted by two verifiers with reproduction); the cross-language tie-order golden remains closed at the unit level because a tie in one runtime is a near-tie in another",
      "Review closeout hardening after a 3-agent verification pass falsified the timestamp-parity claim: the Rust timestamp parser is now a structural mirror of CPython's fromisoformat algorithm itself (separator-finder heuristics incl. the week-date digit-run parity rule, first-of-any tz scan with Z-must-be-final, sticky basic/extended time modes, one-junk-char-before-offset tolerance, min(remaining,6) fraction quotas with any-length pre-offset tails, offset components unchecked but total < 24h, hour-24 rollover on the RAW day number so 2026-03-00T24:00 parses, Python's year 1..9999 range). Acceptance parity is a pinned contract: committed timestamp_battery.json (~400 shapes, verdicts computed by the reference interpreter, replayed by cargo AND a pytest freshness gate) plus ~200k differential-fuzz shapes across three rounds with zero divergence; an IDR_RT_EXTRA_BATTERY env hook keeps large sweeps reproducible. Also fixed from that pass: stale parity.rs tolerance header and the crate README's contradictory timestamp paragraph",
      "Production-readiness assessment + operational logging: honest reports/PRODUCTION_READINESS.md separating strong software readiness from blocked detector readiness (the wall is real labeled data + a realized runtime, not code); added observability.py (opt-in JSON-lines stderr via CLI --log-level, model-load provenance, finding summaries with timing, clean operational errors on missing/corrupt checkpoints), NullHandler so library import stays silent. 118 py tests",
      "Real-data validation gate (production track chosen by Lee): idr-intelligence validate + validation.py. On a strictly time-ordered holdout it re-fits affine calibration and selects an operating threshold at --target-fpr on the earlier segment, snapshots a real-traffic drift baseline (feature_snapshot), and verifies ROC-AUC/ECE/recall + the REALIZED FPR-at-threshold on the untouched later segment; configurable --gate KEY=VALUE risk-appetite thresholds become a go/no-go verdict, a model card, and (only on go) a recalibrated checkpoint. Exits 2 on no-go so a deploy pipeline halts. Honesty is structural: provenance is operator-attested and a verdict is BINDING only when it names real data (synthetic/sim/standin/demo -> non-binding wiring check); unknown --gate keys are rejected rather than silently ignored. Reuses windows_to_batch/affine_calibration_params/feature_snapshot/_evaluate so no scoring logic is duplicated. 125 py tests (adds 7 validation + 3 observability)",
      "ATT&CK finding enrichment (cross-language): observed_attack_stages now carries the real MITRE technique_name (e.g. T1041 -> 'Exfiltration Over C2 Channel'). Centralized the stage dict in attack.stage_record() so the batch/streaming/ONNX-runner paths can't drift; enriched the export manifest's kind_to_attack; propagated technique_name through the Rust manifest, Stage, and observe() so the golden-pinned parity holds. Also fixed a latent flake in test_streaming_matches_batch_scoring (unseeded model init made it order-dependent at top-k tie boundaries) by seeding. 129 py + rust/wire suites green."
    ],
    "active": [],
    "blocked": []
  },
  "evidence": {
    "commands_run": [
      {
        "cmd": "python -m pytest -q",
        "when": "2026-07-23",
        "result": "99 passed"
      },
      {
        "cmd": "ruff check src tests && python -m mypy src",
        "when": "2026-07-23",
        "result": "clean; 18 source files"
      },
      {
        "cmd": "idr-intelligence benchmark --manifest benchmarks/v1.json",
        "when": "2026-07-23",
        "result": "passed, 0 violations (relational_head extraction is pure code motion; forward bit-identical)"
      },
      {
        "cmd": "python -m pytest -q",
        "when": "2026-07-24",
        "result": "107 passed"
      },
      {
        "cmd": "ruff check src tests && python -m mypy src",
        "when": "2026-07-24",
        "result": "clean; 19 source files"
      },
      {
        "cmd": "idr-intelligence benchmark --manifest benchmarks/v1.json",
        "when": "2026-07-24",
        "result": "passed, 0 violations (nan_to_num->where(isnan) and stable ranking are behavior-preserving)"
      },
      {
        "cmd": "cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test (rust/idr-intelligence-rt)",
        "when": "2026-07-24",
        "result": "clean; 6/6 tests incl. golden parity within 1e-4 and exact discrete-field equality"
      },
      {
        "cmd": "cargo test (rust/idr-common-parity, machine-local vs /home/lee/Desktop/CYBER/idr-main)",
        "when": "2026-07-24",
        "result": "3/3: idr_common wire format round-trips through the bridge; note published github idr repo is stale vs local tree (lacks ExternalTriage/TriageClassification/BgpAnomaly)"
      },
      {
        "cmd": "Workflow w17-adversarial-review (65 agents, 5 lenses, adversarial 2-refuter verification)",
        "when": "2026-07-25",
        "result": "30 raw findings, 19 confirmed, 11 refuted; all confirmed findings fixed or explicitly deferred with documentation"
      },
      {
        "cmd": "python -m pytest -q && ruff && python -m mypy src && benchmark && cargo fmt/clippy/test (both crates)",
        "when": "2026-07-25",
        "result": "114 python tests, 12 rust bridge tests, 3 idr-common wire tests, benchmark floors green after review fixes"
      },
      {
        "cmd": "full gate sweep under pinned rustc 1.96.1 (pytest/ruff/mypy/benchmark/cargo fmt+clippy+test/idr-common-parity)",
        "when": "2026-07-26",
        "result": "114 python + 14 rust + 3 wire tests green; goldens regenerated with 3 exotic-timestamp edge events; tract matches goldens at 2e-6"
      },
      {
        "cmd": "differential fuzz: Python fromisoformat vs Rust mirror (12k + 77k + 116k shapes, three seeds/mutation strategies)",
        "when": "2026-07-26",
        "result": "zero divergence after mirroring the reference algorithm; every discovered mechanism pinned in the committed battery"
      },
      {
        "cmd": "idr-intelligence validate --data <windows> --data-provenance soc-2026Q2 --gate max_ece=0.6 --model-card CARD.md --out-weights validated.pt",
        "when": "2026-07-26",
        "result": "end-to-end go verdict on a simulator stand-in: binding=true on real-named provenance, model card + recalibrated checkpoint written, structured validation_complete log emitted, exit 0; default ECE gate correctly yields no-go + exit 2 on the same undersized/overconfident model"
      },
      {
        "cmd": "validate KIND_TO_ATTACK against MITRE ATT&CK Enterprise STIX (/home/lee/Desktop/cti)",
        "when": "2026-07-26",
        "result": "all 11 hand-typed mappings tactic-consistent with real MITRE; TACTIC_ORDER regrounded to the canonical 14-tactic matrix order (added reconnaissance + resource-development); 128 py tests + 6 rust green after fixture regen"
      }
    ],
    "tests": [
      "tests: 125 python (adds validation gate: go on relaxed gates but non-binding on synthetic provenance, binding only on attested real provenance, no-go when a gate fails, single-class-segment rejection, unknown-gate rejection, CLI roundtrip writing card + recalibrated checkpoint, CLI exit-2 on no-go; observability: structured stderr without touching stdout, quiet by default, clean model-load error) + 15 rust bridge tests + 3 machine-local wire tests"
    ],
    "benchmarks": [
      {
        "run": "demo --samples 80 --epochs 3, seed 7, 2026-07-23, attention pooling default",
        "roc_pr": "all five variants saturate at 1.0 (synthetic separability; Phase 2 W6 exists to fix this)",
        "brier": {
          "static_baseline": 0.232802,
          "s6_only": 0.242825,
          "gnn_only": 0.229365,
          "s6_gnn": 0.215886,
          "s6_gnn_uniform_pool": 0.206123
        },
        "evidence_precision_at_5": {
          "static_baseline": 0.8,
          "s6_only": 0.6,
          "gnn_only": 0.6,
          "s6_gnn": 0.6,
          "s6_gnn_uniform_pool": 0.6
        },
        "note": "ep@5 chance floor ~0.5 on 9-node graphs; 3 epochs is deliberately minimal \u2014 metric exists to make Phase 2 improvements measurable"
      },
      {
        "run": "demo --samples 80 --epochs 3, seed 7, 2026-07-23, W3 calibrated",
        "brier": {
          "static_baseline": 0.1855,
          "s6_only": 0.2211,
          "gnn_only": 0.1567,
          "s6_gnn": 0.1279,
          "s6_gnn_uniform_pool": 0.0813
        },
        "ece": {
          "s6_gnn": 0.3368,
          "s6_gnn_uniform_pool": 0.2843
        },
        "note": "all arms select T=0.1995 (grid edge) \u2014 demo models are systematically underconfident at 3 epochs; interior T expected on real training budgets"
      },
      {
        "run": "demo 80/3 seed 7, 2026-07-23, W6 scenario generalization (hybrid, v0_easy-trained)",
        "brier_by_scenario": {
          "v0_easy": 0.1279,
          "graded": 0.1665,
          "distractor": 0.3348,
          "legit_update": 0.1288,
          "truncated": 0.1314,
          "low_and_slow": 0.1374,
          "split_host": 0.2018,
          "hash_rotation": 0.1948
        },
        "note": "first measurable cross-scenario differentiation; ranking metrics still saturate \u2014 W9 replicates and larger eval sets sharpen this"
      },
      {
        "run": "ablation 60/2 folds=3 replicates=3 seed 7, 2026-07-23",
        "verdict": "tie",
        "mean_brier": {
          "gnn_only": 0.1659,
          "s6_gnn_uniform_pool": 0.1899,
          "s6_gnn": 0.1901,
          "s6_only": 0.21,
          "static_baseline": 0.218
        },
        "note": "margin 0.024 < paired std 0.124 \u2014 the statistical machinery correctly refuses to crown a winner at this budget"
      }
    ]
  },
  "risks": {
    "defects": [],
    "assumptions": [
      "Simulator class separability makes ranking metrics uninformative at n<=80; only calibration (Brier/ECE) and cross-scenario Brier separate variants \u2014 W6 provides the harder families",
      "predicted_next_stage / observed_attack_stages come from a deterministic ATT&CK table (attack.py), not the model \u2014 auditable by design",
      "evidence_precision_at_5 uses multi-event entity membership as ground truth; valid for the current simulator convergence design only"
    ],
    "uncertainty": [
      "No real labeled campaigns yet (W8 path exists); production accuracy unknown",
      "Homogeneous message passing; typed variant deferred until split-host evasion can falsify it",
      "VERIFIED via timing_only (identical structure, timing-only discriminator): temporal physics (W11/W12) add no measurable value on synthetic data because the scalar delta feature already captures timing; global time_mode beats time_aware. The machinery is architecturally correct but needs real multi-entity streams to prove out (--data path). See reports/AUDIT.md"
    ]
  },
  "artifacts": [
    {
      "path": "docs/ROADMAP.md",
      "role": "canonical 4-phase/14-workstream improvement roadmap with judge scores and rejections"
    },
    {
      "path": "docs/ARCHITECTURE.md",
      "role": "integration design + Rust EventKind sketch"
    },
    {
      "path": "reports/AUDIT.md",
      "role": "model-risk audit and known limitations"
    },
    {
      "path": ".github/workflows/ci.yml",
      "role": "Arch container CI: install, import check, pytest, ruff, mypy"
    },
    {
      "path": "artifacts/hybrid_model.pt",
      "role": "checkpoint v2 (generated by demo, gitignored)"
    },
    {
      "path": "rust/idr-intelligence-rt/",
      "role": "Rust serving bridge crate (tract runtime, CLI mirroring `stream`, golden parity tests)"
    },
    {
      "path": "rust/idr-intelligence-rt/tests/fixtures/generate.py",
      "role": "deterministic fixture generator: bundle + golden streams + feature vectors; self-asserts ORT==torch and top-k gap floor"
    },
    {
      "path": "rust/idr-common-parity/",
      "role": "machine-local integration crate vs real idr_common (path-dep on idr-main tree; not in CI)"
    },
    {
      "path": "reports/PRODUCTION_READINESS.md",
      "role": "software-vs-detector readiness assessment; staged go-live path (deployment hardening -> real-data validation gate -> real data -> runtime -> integration -> shadow)"
    },
    {
      "path": "src/idr_intelligence/data/attack_reference.json",
      "role": "canonical enterprise tactic order + technique catalog distilled from the MITRE ATT&CK Enterprise STIX bundle (regenerable via scripts/ground_attack_reference.py)"
    }
  ],
  "compressed_state": {
    "format": "performance-intelligence-state-v1",
    "source_digest": "git:2c26ef2-plus-w17",
    "state_vector": [],
    "latest_event": {
      "when": "2026-07-26",
      "summary": "Continuous-improvement loop on real MITRE data: grounded TACTIC_ORDER + validated KIND_TO_ATTACK against MITRE, then enriched every finding with real technique names cross-language (Python + manifest + Rust, golden-pinned). Fixed CI linker, added Rust logging, fixed a latent equivalence-test flake. The escalation model's training-data wall still stands (this data is ATT&CK reference, not labeled campaigns)."
    }
  }
}